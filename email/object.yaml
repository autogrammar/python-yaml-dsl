Message:
  patterns:
    - "{action:create} {} with {public} and {object}"
    - "{action:read} {many} {} with {public}"
    - "{action:delete} {many} {} with {public}"
#    - "{action:read,delete} {many} {} with {public}"
  shell:
    - "{}.sh {action} {public}"
    - "{}.sh {many} {action:read} {public}"
    - "{}.sh {many} {action:delete} {public}"
  public:
    date: datetime
    sender: string
    content: string
    subject: string
  action:
    many:
      - read
      - delete
    one:
      - create
    default:
      - read
  many:
    - last: integer
    - all
  object:
    - Account

Account:
  patterns:
#    - "{} {public}"
    - "{action:disconnect} {} {public}"
    - "{action:connect} to {} {public} and {object}"
  shell:
    - "{}.sh {action} {public}"
    - "{}.sh {many} {action:read} {public}"
    - "{}.sh {many} {action:delete} {public}"
  public:
    email: string
  private:
    server: string
    password: string
    username: string
    port: number
  action:
    one:
      - connect
      - disconnect
    default:
      - connect
    end:
      - connect
  object:
    - Message